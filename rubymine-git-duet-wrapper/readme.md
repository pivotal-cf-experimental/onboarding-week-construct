Allows you to use git duet with the RubyMine git commit interface

In order to use this wrapper, set the "Version Control -> Git -> Path to git executable" preference to
`/usr/local/bin/rubymine-git-wrapper`.
